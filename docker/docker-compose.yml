services:
  redis:
    image: redis:7.4.4
    restart: always
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.18.2
    volumes:
      - esdata:/usr/share/elasticsearch/data
    environment:
      # Comment-out the line below for a cluster of multiple nodes
      - discovery.type=single-node
      # Uncomment the line below below for a cluster of multiple nodes
      # - cluster.name=docker-cluster
      - xpack.ml.enabled=false
      - xpack.security.enabled=false
      - thread_pool.search.queue_size=5000
      - logger.org.elasticsearch.discovery="ERROR"
      - "ES_JAVA_OPTS=-Xms${ELASTIC_MEMORY_SIZE} -Xmx${ELASTIC_MEMORY_SIZE}"
    restart: always
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://elasticsearch:9200/_cluster/health?wait_for_status=yellow >/dev/null || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 50
  minio:
    image: minio/minio:RELEASE.2024-05-28T17-19-04Z # Use "minio/minio:RELEASE.2024-05-28T17-19-04Z-cpuv1" to troubleshoot compatibility issues with CPU
    volumes:
      - s3data:/data
    ports:
      - "9000:9000"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}    
    command: server /data
    restart: always
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      timeout: 5s
      retries: 3
  rabbitmq:
    image: rabbitmq:4.1-management
    environment:
      - RABBITMQ_DEFAULT_USER=${RABBITMQ_DEFAULT_USER}
      - RABBITMQ_DEFAULT_PASS=${RABBITMQ_DEFAULT_PASS}
      - RABBITMQ_NODENAME=rabbit01@localhost
    volumes:
      - type: bind
        source: ./rabbitmq.conf
        target: /etc/rabbitmq/rabbitmq.conf 
      - amqpdata:/var/lib/rabbitmq
    restart: always
    healthcheck:
      test: rabbitmq-diagnostics -q ping
      interval: 30s
      timeout: 30s
      retries: 3
  opencti:
    image: opencti/platform:6.7.3
    environment:
      - NODE_OPTIONS=--max-old-space-size=8096
      - APP__PORT=8080
      - APP__BASE_URL=${OPENCTI_BASE_URL}
      - APP__ADMIN__EMAIL=${OPENCTI_ADMIN_EMAIL}
      - APP__ADMIN__PASSWORD=${OPENCTI_ADMIN_PASSWORD}
      - APP__ADMIN__TOKEN=${OPENCTI_ADMIN_TOKEN}
      - APP__APP_LOGS__LOGS_LEVEL=error
      - REDIS__HOSTNAME=redis
      - REDIS__PORT=6379
      - ELASTICSEARCH__URL=http://elasticsearch:9200
      - ELASTICSEARCH__NUMBER_OF_REPLICAS=0
      - MINIO__ENDPOINT=minio
      - MINIO__PORT=9000
      - MINIO__USE_SSL=false
      - MINIO__ACCESS_KEY=${MINIO_ROOT_USER}
      - MINIO__SECRET_KEY=${MINIO_ROOT_PASSWORD}
      - RABBITMQ__HOSTNAME=rabbitmq
      - RABBITMQ__PORT=5672
      - RABBITMQ__PORT_MANAGEMENT=15672
      - RABBITMQ__MANAGEMENT_SSL=false
      - RABBITMQ__USERNAME=${RABBITMQ_DEFAULT_USER}
      - RABBITMQ__PASSWORD=${RABBITMQ_DEFAULT_PASS}
      - SMTP__HOSTNAME=${SMTP_HOSTNAME}
      - SMTP__PORT=25
      - PROVIDERS__LOCAL__STRATEGY=LocalStrategy
      - APP__HEALTH_ACCESS_KEY=${OPENCTI_HEALTHCHECK_ACCESS_KEY}
    ports:
      - "8080:8080"
    depends_on:
      redis:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy
      minio:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    restart: always
    healthcheck:
      test:  ["CMD", "wget", "-qO-", "http://opencti:8080/health?health_access_key=${OPENCTI_HEALTHCHECK_ACCESS_KEY}"]
      interval: 10s
      timeout: 5s
      retries: 20
  worker:
    image: opencti/worker:6.7.3
    environment:
      - OPENCTI_URL=http://opencti:8080
      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
      - WORKER_LOG_LEVEL=info
    depends_on:
      opencti:
        condition: service_healthy
    deploy:
      mode: replicated
      replicas: 3
    restart: always
  connector-export-file-stix:
    image: opencti/connector-export-file-stix:6.7.3
    environment:
      - OPENCTI_URL=http://opencti:8080
      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
      - CONNECTOR_ID=${CONNECTOR_EXPORT_FILE_STIX_ID} # Valid UUIDv4
      - CONNECTOR_TYPE=INTERNAL_EXPORT_FILE
      - CONNECTOR_NAME=ExportFileStix2
      - CONNECTOR_SCOPE=application/json
      - CONNECTOR_LOG_LEVEL=info
    restart: always
    depends_on:
      opencti:
        condition: service_healthy
  connector-export-file-csv:
    image: opencti/connector-export-file-csv:6.7.3
    environment:
      - OPENCTI_URL=http://opencti:8080
      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
      - CONNECTOR_ID=${CONNECTOR_EXPORT_FILE_CSV_ID} # Valid UUIDv4
      - CONNECTOR_TYPE=INTERNAL_EXPORT_FILE
      - CONNECTOR_NAME=ExportFileCsv
      - CONNECTOR_SCOPE=text/csv
      - CONNECTOR_LOG_LEVEL=info
    restart: always
    depends_on:
      opencti:
        condition: service_healthy
  connector-export-file-txt:
    image: opencti/connector-export-file-txt:6.7.3
    environment:
      - OPENCTI_URL=http://opencti:8080
      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
      - CONNECTOR_ID=${CONNECTOR_EXPORT_FILE_TXT_ID} # Valid UUIDv4
      - CONNECTOR_TYPE=INTERNAL_EXPORT_FILE
      - CONNECTOR_NAME=ExportFileTxt
      - CONNECTOR_SCOPE=text/plain
      - CONNECTOR_LOG_LEVEL=info
    restart: always
    depends_on:
      opencti:
        condition: service_healthy
  connector-import-file-stix:
    image: opencti/connector-import-file-stix:6.7.3
    environment:
      - OPENCTI_URL=http://opencti:8080
      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
      - CONNECTOR_ID=${CONNECTOR_IMPORT_FILE_STIX_ID} # Valid UUIDv4
      - CONNECTOR_TYPE=INTERNAL_IMPORT_FILE
      - CONNECTOR_NAME=ImportFileStix
      - CONNECTOR_VALIDATE_BEFORE_IMPORT=true # Validate any bundle before import
      - CONNECTOR_SCOPE=application/json,text/xml
      - CONNECTOR_AUTO=true # Enable/disable auto-import of file
      - CONNECTOR_LOG_LEVEL=info
    restart: always
    depends_on:
      opencti:
        condition: service_healthy
  connector-import-document:
    image: opencti/connector-import-document:6.7.3
    environment:
      - OPENCTI_URL=http://opencti:8080
      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
      - CONNECTOR_ID=${CONNECTOR_IMPORT_DOCUMENT_ID} # Valid UUIDv4
      - CONNECTOR_TYPE=INTERNAL_IMPORT_FILE
      - CONNECTOR_NAME=ImportDocument
      - CONNECTOR_VALIDATE_BEFORE_IMPORT=true # Validate any bundle before import
      - CONNECTOR_SCOPE=application/pdf,text/plain,text/html
      - CONNECTOR_AUTO=true # Enable/disable auto-import of file
      - CONNECTOR_ONLY_CONTEXTUAL=false # Only extract data related to an entity (a report, a threat actor, etc.)
      - CONNECTOR_CONFIDENCE_LEVEL=15 # From 0 (Unknown) to 100 (Fully trusted)
      - CONNECTOR_LOG_LEVEL=info
      - IMPORT_DOCUMENT_CREATE_INDICATOR=true
    restart: always
    depends_on:
      opencti:
        condition: service_healthy
  connector-analysis:
    image: opencti/connector-import-document:6.7.3
    environment:
      - OPENCTI_URL=http://opencti:8080
      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
      - CONNECTOR_ID=${CONNECTOR_ANALYSIS_ID} # Valid UUIDv4
      - CONNECTOR_TYPE=INTERNAL_ANALYSIS
      - CONNECTOR_NAME=ImportDocumentAnalysis
      - CONNECTOR_VALIDATE_BEFORE_IMPORT=false # Validate any bundle before import
      - CONNECTOR_SCOPE=application/pdf,text/plain,text/html
      - CONNECTOR_AUTO=true # Enable/disable auto-import of file
      - CONNECTOR_ONLY_CONTEXTUAL=false # Only extract data related to an entity (a report, a threat actor, etc.)
      - CONNECTOR_CONFIDENCE_LEVEL=15 # From 0 (Unknown) to 100 (Fully trusted)
      - CONNECTOR_LOG_LEVEL=info
    restart: always
    depends_on:
      opencti:
        condition: service_healthy
#  connector-abuse-ssl:
#    image: opencti/connector-abuse-ssl:6.7.3
#    environment:
#      - OPENCTI_URL=http://opencti:8080
#      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
#      - CONNECTOR_ID=26bdd02c-3797-40eb-92c4-e545f040eabd
#      - "CONNECTOR_NAME=Abuse.ch SSL Blacklist"
#      - CONNECTOR_SCOPE=abusessl
#      - CONNECTOR_LOG_LEVEL=error
#      - ABUSESSL_URL=https://sslbl.abuse.ch/blacklist/sslipblacklist.csv
#      - ABUSESSL_INTERVAL=360 # Time to wait in minutes between subsequent requests
#    restart: always
  connector-abuseipdb-ipblacklist:
    image: opencti/connector-abuseipdb-ipblacklist:6.6.17
    environment:
      - OPENCTI_URL=http://opencti:8080
      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
      - CONNECTOR_ID=e6ee3c9f-3602-4832-9c0d-3c12da79f09f
      - "CONNECTOR_NAME=AbuseIPDB IP Blacklist"
      - CONNECTOR_SCOPE=abuseipdb
      - CONNECTOR_LOG_LEVEL=error
      - ABUSEIPDB_URL=https://api.abuseipdb.com/api/v2/blacklist
      - ABUSEIPDB_API_KEY=${ABUSEIPDB_API_KEY}
      - ABUSEIPDB_SCORE=100
      - ABUSEIPDB_LIMIT=10000
      - ABUSEIPDB_INTERVAL=2 #Day
    restart: always
  connector-alienvault:
    image: opencti/connector-alienvault:6.6.17
    environment:
      - OPENCTI_URL=http://opencti:8080
      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
      - CONNECTOR_ID=4b7785aa-1746-43fe-bb4d-38a934b78d34
      - CONNECTOR_NAME=AlienVault
      - CONNECTOR_SCOPE=alienvault
      - CONNECTOR_LOG_LEVEL=error
      - CONNECTOR_DURATION_PERIOD=PT30M # In ISO8601 Format starting with "P" for Period ex: "PT30M" = Period time of 30 minutes
      - ALIENVAULT_BASE_URL=https://otx.alienvault.com
      - ALIENVAULT_API_KEY=${ALIENVAULT_API_KEY}
      - ALIENVAULT_TLP=White
      - ALIENVAULT_CREATE_OBSERVABLES=true
      - ALIENVAULT_CREATE_INDICATORS=true
      - ALIENVAULT_PULSE_START_TIMESTAMP=2025-07-01T00:00:00                  # BEWARE! Could be a lot of pulses!
      - ALIENVAULT_REPORT_TYPE=threat-report
      - ALIENVAULT_REPORT_STATUS=New
      - ALIENVAULT_GUESS_MALWARE=false                                        # Use tags to guess malware.
      - ALIENVAULT_GUESS_CVE=false                                            # Use tags to guess CVE.
      - ALIENVAULT_EXCLUDED_PULSE_INDICATOR_TYPES=FileHash-MD5,FileHash-SHA1  # Excluded Pulse indicator types.
      - ALIENVAULT_ENABLE_RELATIONSHIPS=true                                  # Enable/Disable relationship creation between SDOs.
      - ALIENVAULT_ENABLE_ATTACK_PATTERNS_INDICATES=false                     # Enable/Disable "indicates" relationships between indicators and attack patterns
      - ALIENVAULT_INTERVAL_SEC=1800
      - ALIENVAULT_DEFAULT_X_OPENCTI_SCORE=50
      - ALIENVAULT_X_OPENCTI_SCORE_IP=60
      - ALIENVAULT_X_OPENCTI_SCORE_DOMAIN=70
      - ALIENVAULT_X_OPENCTI_SCORE_HOSTNAME=75
      - ALIENVAULT_X_OPENCTI_SCORE_EMAIL=70
      - ALIENVAULT_X_OPENCTI_SCORE_FILE=85
      - ALIENVAULT_X_OPENCTI_SCORE_URL=80
      - ALIENVAULT_X_OPENCTI_SCORE_MUTEX=60
      - ALIENVAULT_X_OPENCTI_SCORE_CRYPTOCURRENCY_WALLET=80
    restart: always
#  connector-cisa-known-exploited-vulnerabilities:
#    image: opencti/connector-cisa-known-exploited-vulnerabilities:6.7.3
#    environment:
#      - OPENCTI_URL=http://opencti:8080
#      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
#      - CONNECTOR_ID=7eeee7a4-7fbe-4f75-9116-a622364a443a
#      - "CONNECTOR_NAME=CISA Known Exploited Vulnerabilities"
#      - CONNECTOR_SCOPE=cisa
#      - CONNECTOR_RUN_AND_TERMINATE=false
#      - CONNECTOR_LOG_LEVEL=error
#      - CONNECTOR_DURATION_PERIOD=P2D
#      - CISA_CATALOG_URL=https://www.cisa.gov/sites/default/files/feeds/known_exploited_vulnerabilities.json
#      - CISA_CREATE_INFRASTRUCTURES=false
#      - CISA_TLP=TLP:CLEAR
#    restart: always
#  connector-cyber-campaign-collection:
#    image: opencti/connector-cyber-campaign-collection:6.7.3
#    environment:
#      - OPENCTI_URL=http://opencti:8080
#      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
#      - CONNECTOR_ID=d58dfc4b-daac-430a-81c8-b34970386a87
#      - "CONNECTOR_NAME=APT & Cybercriminals Campaign Collection"
#      - CONNECTOR_SCOPE=report
#      - CONNECTOR_RUN_AND_TERMINATE=false
#      - CONNECTOR_LOG_LEVEL=error
#      - CYBER_MONITOR_GITHUB_TOKEN= # If not provided, rate limit will be very low
#      - CYBER_MONITOR_FROM_YEAR=2018
#      - CYBER_MONITOR_INTERVAL=4 # In days, must be strictly greater than 1
#    restart: always
  connector-cve:
    image: opencti/connector-cve:6.6.17
    environment:
      - OPENCTI_URL=http://opencti:8080
      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
      - CONNECTOR_ID=d76d38a2-f054-4b68-a335-243c35131ef8
      - CONNECTOR_NAME=Common Vulnerabilities and Exposures
      - CONNECTOR_SCOPE=identity,vulnerability
      - CONNECTOR_RUN_AND_TERMINATE=false
      - CONNECTOR_LOG_LEVEL=error
      - CVE_BASE_URL=https://services.nvd.nist.gov/rest/json/cves
      - CVE_API_KEY=${CVE_API_KEY} # Required
      - CVE_INTERVAL=2 # Required, in hours advice min 2
      - CVE_MAX_DATE_RANGE=120 # In days, max 120
      - CVE_MAINTAIN_DATA=true # Required, retrieve only updated data
      - CVE_PULL_HISTORY=false # If true, CVE_HISTORY_START_YEAR is required
      - CVE_HISTORY_START_YEAR=2019 # Required if pull_history is True, min 2019 (see documentation CVE and CVSS base score V3.1)
    restart: always
#  connector-malpedia:
#    image: opencti/connector-malpedia:6.7.3
#    environment:
#      - OPENCTI_URL=http://opencti:8080
#      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
#      - CONNECTOR_ID=aad9d7ee-7c28-4fa2-a868-cf40696494f3
#      - CONNECTOR_NAME=Malpedia
#      - CONNECTOR_SCOPE=malpedia
#      - CONNECTOR_LOG_LEVEL=error
#      - CONNECTOR_EXPOSE_METRICS=false
#      - "MALPEDIA_AUTH_KEY= # Empty key only fetches TLP:WHITE information"
#      - MALPEDIA_INTERVAL_SEC=86400 # Run once every day
#      - MALPEDIA_IMPORT_INTRUSION_SETS=true
#      - MALPEDIA_IMPORT_YARA=true
#      - MALPEDIA_CREATE_INDICATORS=true
#      - MALPEDIA_CREATE_OBSERVABLES=true
#    restart: always
#  connector-malware-bazaar:
#    image: opencti/connector-malwarebazaar:6.7.3
#    environment:
#      # Connector's generic execution parameters
#      - OPENCTI_URL=http://opencti:8080
#      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
#      # Connector's definition parameters REQUIRED
#      - CONNECTOR_ID=46920578-5296-4c93-a90a-49c56d2b4e0a
#      - CONNECTOR_TYPE=EXTERNAL_IMPORT
#      - CONNECTOR_NAME=MalwareBazaar
#      - CONNECTOR_SCOPE=StixFile
#      - CONNECTOR_LOG_LEVEL=info
#      - CONNECTOR_DURATION_PERIOD=PT50M # ISO8601 format in String, start with 'P...' for Period
#      # Connector's definition parameters OPTIONAL
#      # - CONNECTOR_QUEUE_THRESHOLD=500 # Default 500Mo, Float accepted
#      # - CONNECTOR_RUN_AND_TERMINATE=False # Default False, True run connector once
#      # - CONNECTOR_SEND_TO_QUEUE=True # Default True
#      # - CONNECTOR_SEND_TO_DIRECTORY=False # Default False
#      # - CONNECTOR_SEND_TO_DIRECTORY_PATH=CHANGEME # if CONNECTOR_SEND_TO_DIRECTORY is True, you must specify a path
#      # - CONNECTOR_SEND_TO_DIRECTORY_RETENTION=7 # Default 7, in days
#      # Connector's custom execution parameters
#      - MALWAREBAZAAR_API_BASE_URL=https://mb-api.abuse.ch/api/v1/
#      # - MALWAREBAZAAR_API_KEY=${MALWAREBAZAAR_USER_TOKEN} # Stored in ENVAR, but can be hardcoded. Do not check TOKEN into version control. Not required in this implementation, left here for reference if ever needed.
#      - MALWAREBAZAAR_TLP_LEVEL=clear # available values are: clear, white, green, amber, amber+strict, red - Default: 'clear'
#      - MALWAREBAZAAR_X_OPENCTI_SCORE=100 # Range is 0 - 100, will degrade over time see: https://docs.opencti.io/latest/usage/indicators-lifecycle/#score-decay
#      - MALWAREBAZAAR_INCLUDE_TAGS=exe,dll,docm,docx,doc,xls,xlsx,xlsm,js # (Optional) Only download files if any tag matches. (Comma separated)
#      - MALWAREBAZAAR_INCLUDE_REPORTERS= # (Optional) Only download files uploaded by these reporters. (Comma separated)
#      - MALWAREBAZAAR_LABELS=malware-bazaar # (Optional) Labels to apply to uploaded Artifacts. (Comma separated)
#    restart: always
#  connector-mitre-atlas:
#    image: opencti/connector-mitre-atlas:6.7.3
#    environment:
#      - OPENCTI_URL=http://opencti:8080
#      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
#      - CONNECTOR_ID=8d515f82-2e51-4063-ba71-2c2f5ddecb8b
#      - "CONNECTOR_NAME=MITRE ATLAS"
#      - CONNECTOR_SCOPE=identity,attack-pattern,course-of-action,relationship,x-mitre-collection,x-mitre-matrix,x-mitre-tactic
#      - CONNECTOR_RUN_AND_TERMINATE=false
#      - CONNECTOR_LOG_LEVEL=error
#      - MITRE_ATLAS_URL=https://raw.githubusercontent.com/mitre-atlas/atlas-navigator-data/main/dist/stix-atlas.json
#      - MITRE_ATLAS_INTERVAL=7 # In days, must be strictly greater than 1
#    restart: always
#  connector-mitre:
#    image: opencti/connector-mitre:6.7.3
#    environment:
#      - OPENCTI_URL=http://opencti:8080
#      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
#      - CONNECTOR_ID=5632e036-3f55-4a76-a499-475170ac4949
#      - "CONNECTOR_NAME=MITRE Datasets"
#      - CONNECTOR_SCOPE=tool,report,malware,identity,campaign,intrusion-set,attack-pattern,course-of-action,x-mitre-data-source,x-mitre-data-component,x-mitre-matrix,x-mitre-tactic,x-mitre-collection
#      - CONNECTOR_RUN_AND_TERMINATE=false
#      - CONNECTOR_LOG_LEVEL=error
#      - MITRE_REMOVE_STATEMENT_MARKING=true
#      - MITRE_INTERVAL=7 # In days
#    restart: always
#  connector-opencti:
#    image: opencti/connector-opencti:6.7.3
#    environment:
#      - OPENCTI_URL=http://opencti:8080
#      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
#      - CONNECTOR_ID=83a020b6-6662-44c3-9029-7449f1cd7d95
#      - "CONNECTOR_NAME=OpenCTI Datasets"
#      - CONNECTOR_SCOPE=marking-definition,identity,location
#      - CONNECTOR_UPDATE_EXISTING_DATA=true
#      - CONNECTOR_RUN_AND_TERMINATE=false
#      - CONNECTOR_LOG_LEVEL=error
#      - CONFIG_SECTORS_FILE_URL=https://raw.githubusercontent.com/OpenCTI-Platform/datasets/master/data/sectors.json
#      - CONFIG_GEOGRAPHY_FILE_URL=https://raw.githubusercontent.com/OpenCTI-Platform/datasets/master/data/geography.json
#      - CONFIG_COMPANIES_FILE_URL=https://raw.githubusercontent.com/OpenCTI-Platform/datasets/master/data/companies.json
#      - CONFIG_REMOVE_CREATOR=false
#      - CONFIG_INTERVAL=7 # In days
#    restart: always
#  connector-redflag-domains:
#    image: opencti/connector-red-flag-domains:6.7.3
#    environment:
#      - OPENCTI_URL=http://opencti:8080
#      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
#      - CONNECTOR_ID=18ca8c97-3938-4add-8dcb-87144381012a
#      - "CONNECTOR_NAME=Red Flag Domains"
#      - CONNECTOR_SCOPE=red-flag-domains
#      - UPDATE_EXISTING_DATA=False
#      - CONNECTOR_LOG_LEVEL=info
#      - REDFLAGDOMAINS_URL=https://dl.red.flag.domains/daily/
#    restart: always
#  connector-stopforumspam:
#    image: opencti/connector-stopforumspam:6.7.3
#    environment:
#      - OPENCTI_URL=http://opencti:8080
#      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
#      - CONNECTOR_ID=97651ca2-d858-4da8-9eb5-9bbf67030921
#      - CONNECTOR_NAME=Stopforumspam
#      - CONNECTOR_SCOPE=stopforumspam
#      - CONNECTOR_LOG_LEVEL=error
#      - STOPFORUMSPAM_URL=https://www.stopforumspam.com/downloads/toxic_domains_whole_filtered_50000.txt
#      - STOPFORUMSPAM_CREATE_INDICATORS=true
#      - STOPFORUMSPAM_INTERVAL=3 # In days, must be strictly greater than 1
#    restart: always
#  connector-threatfox:
#    image: opencti/connector-threatfox:6.7.3
#    environment:
#      - OPENCTI_URL=http://opencti:8080
#      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
#      - CONNECTOR_ID=11b470f3-d1ab-4e89-9b9a-6f4c15ab2b3e
#      - "CONNECTOR_NAME=Abuse.ch | ThreatFox"
#      - CONNECTOR_SCOPE=ThreatFox
#      - CONNECTOR_LOG_LEVEL=error
#      - THREATFOX_CSV_URL=https://threatfox.abuse.ch/export/csv/recent/
#      - THREATFOX_IMPORT_OFFLINE=true
#      - THREATFOX_CREATE_INDICATORS=true
#      - THREATFOX_DEFAULT_X_OPENCTI_SCORE=50
#      - THREATFOX_X_OPENCTI_SCORE_IP=60
#      - THREATFOX_X_OPENCTI_SCORE_DOMAIN=70
#      - THREATFOX_X_OPENCTI_SCORE_URL=75
#      - THREATFOX_X_OPENCTI_SCORE_HASH=80
#      - THREATFOX_INTERVAL=3 # In days, must be strictly greater than 1
#      - THREATFOX_IOC_TO_IMPORT=ip:port,domain,url # List of IOC types to import
#    restart: always
#  connector-urlhaus-recent-payloads:
#    image: opencti/connector-urlhaus-recent-payloads:6.7.3
#    environment:
#      - OPENCTI_URL=http://opencti:8080
#      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
#      - CONNECTOR_ID=48032a03-bfa2-4a55-a8f6-e61763b56650
#      - "CONNECTOR_NAME=URLhaus Recent Payloads"
#      - CONNECTOR_LOG_LEVEL=error
#      - URLHAUS_RECENT_PAYLOADS_API_URL=https://urlhaus-api.abuse.ch/v1/
#      - URLHAUS_RECENT_PAYLOADS_COOLDOWN_SECONDS=300 # Time to wait in seconds between subsequent requests
#      - URLHAUS_RECENT_PAYLOADS_INCLUDE_FILETYPES=exe,dll,docm,docx,doc,xls,xlsx,xlsm,js,xll # (Optional) Only download files if any tag matches. (Comma separated)
#      - URLHAUS_RECENT_PAYLOADS_INCLUDE_SIGNATURES= # (Optional) Only download files matching these Yara rules. (Comma separated)
#      - URLHAUS_RECENT_PAYLOADS_SKIP_UNKNOWN_FILETYPES=true # Skip files with an unknown file type
#      - URLHAUS_RECENT_PAYLOADS_SKIP_NULL_SIGNATURE=true # Skip files that didn't match known Yara rules
#      - URLHAUS_RECENT_PAYLOADS_LABELS=urlhaus # (Optional) Labels to apply to uploaded Artifacts. (Comma separated)
#      - URLHAUS_RECENT_PAYLOADS_LABELS_COLOR=#54483b
#      - URLHAUS_RECENT_PAYLOADS_SIGNATURE_LABEL_COLOR=#0059f7 # Color for Yara rule match label
#      - URLHAUS_RECENT_PAYLOADS_FILETYPE_LABEL_COLOR=#54483b # Color to use for filetype label
#    restart: always
#  connector-urlhaus:
#    image: opencti/connector-urlhaus:6.7.3
#    environment:
#      - OPENCTI_URL=http://opencti:8080
#      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
#      - CONNECTOR_ID=84637d5d-bfc2-4447-9606-e05a0ccf1766
#      - "CONNECTOR_NAME=Abuse.ch URLhaus"
#      - CONNECTOR_SCOPE=urlhaus
#      - CONNECTOR_LOG_LEVEL=error
#      - URLHAUS_CSV_URL=https://urlhaus.abuse.ch/downloads/csv_recent/
#      - URLHAUS_DEFAULT_X_OPENCTI_SCORE=80 # Optional: Defaults to 80.
#      - URLHAUS_IMPORT_OFFLINE=true
#      - URLHAUS_THREATS_FROM_LABELS=true
#      - URLHAUS_INTERVAL=3 # In days, must be strictly greater than 1
#    restart: always
  connector-urlscan:
    image: opencti/connector-urlscan:6.6.17
    environment:
      - OPENCTI_URL=http://opencti:8080
      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
      - CONNECTOR_ID=c4ee6394-81f6-4689-a4e6-de43d4d1f8a7
      - CONNECTOR_NAME=Urlscan
      - CONNECTOR_SCOPE=urlscan
      - CONNECTOR_LOG_LEVEL=error
      - CONNECTOR_CREATE_INDICATORS=true
      - CONNECTOR_TLP=white
      - CONNECTOR_LABELS=Phishing,Phishfeed
      - CONNECTOR_INTERVAL=86400 # seconds, 1d
      - CONNECTOR_LOOKBACK=3 # days
      - URLSCAN_URL=https://urlscan.io/api/v1/pro/phishfeed?format=json
      - URLSCAN_API_KEY=${URLSCAN_API_KEY}
      - URLSCAN_DEFAULT_X_OPENCTI_SCORE=50
    restart: always
  connector-vulncheck:
    image: opencti/connector-vulncheck:6.6.17
    environment:
      - OPENCTI_URL=http://opencti:8080
      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
      - CONNECTOR_ID=a8db256c-e355-4650-b8db-6844f36fb123
      # - CONNECTOR_SCOPE=vulnerability,malware,threat-actor,infrastructure,location,ip-addr,indicator,external-reference,software
      - CONNECTOR_VULNCHECK_API_KEY=${CONNECTOR_VULNCHECK_API_KEY}
      # - CONNECTOR_VULNCHECK_DATA_SOURCES=botnets,epss,exploits,initial-access,ipintel,nist-nvd2,ransomware,snort,suricata,threat-actors,vulncheck-kev,vulncheck-nvd2
    restart: always
#  connector-wiz:
#    image: opencti/connector-wiz:6.7.3
#    environment:
#      # Connector's generic execution parameters
#      - OPENCTI_URL=http://opencti:8080
#      - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
#      # Connector's definition parameters REQUIRED
#      - CONNECTOR_ID=644eee28-d983-488c-a33d-2bb8b9fe1064
#      - CONNECTOR_NAME=Wiz Cloud Threat LandScape
#      - CONNECTOR_SCOPE=wiz
#      - CONNECTOR_LOG_LEVEL=error
#      - CONNECTOR_DURATION_PERIOD=PT30M # ISO8601 format in String, start with 'P...' for Period
#      - WIZ_THREAT_ACTOR_AS_INTRUSION_SET=False
#      - WIZ_TLP_LEVEL=clear
#      # Connector's definition parameters OPTIONAL
#      # - CONNECTOR_QUEUE_THRESHOLD=500 # Default 500Mo, Float accepted
#      # - CONNECTOR_RUN_AND_TERMINATE=False # Default False, True run connector once
#      # - CONNECTOR_SEND_TO_QUEUE=True # Default True
#      # - CONNECTOR_SEND_TO_DIRECTORY=False # Default False
#      # - CONNECTOR_SEND_TO_DIRECTORY_PATH=CHANGEME # if CONNECTOR_SEND_TO_DIRECTORY is True, you must specify a path
#      # - CONNECTOR_SEND_TO_DIRECTORY_RETENTION=7 # Default 7, in days
#    restart: always

volumes:
  esdata:
  s3data:
  redisdata:
  amqpdata:

